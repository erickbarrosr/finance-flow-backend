spring:
  application:
    name: finance-flow
  profiles:
    active: ${APP_PROFILE:local}
  jpa:
    open-in-view: false

cors:
  origins: ${CORS_ORIGINS:http://localhost:4200}

jwt:
  secret: ${JWT_SECRET:açskjreiurvncklvnaoriqwunkvça}
  expiration: ${JWT_EXPIRATION_MS:3600000}
